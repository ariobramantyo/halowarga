import 'package:flutter/material.dart';

class SuratPageWarga extends StatelessWidget {
  const SuratPageWarga({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Icon(Icons.description),
      ),
    );
  }
}
