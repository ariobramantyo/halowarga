import 'package:flutter/material.dart';

class SettingPageWarga extends StatelessWidget {
  const SettingPageWarga({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Icon(Icons.settings),
      ),
    );
  }
}
